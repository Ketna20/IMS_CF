import React from 'react';
import classes from './Product.module.css';

const product = (props) => {
    return (
        <div className={classes.Product}>

        </div>
    );
};
   
// Here i will have the each product's information
// product code, product short desc, long desc, 
// category(flower, plant, hardgood, balloon, Plush, etc.)
// units/pack (st/bunch,  vase/case) etc.
